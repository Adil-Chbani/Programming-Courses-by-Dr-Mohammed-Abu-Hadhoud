#include <iostream>
#include <string>
#include <vector>
using namespace std;

string JoinString(vector<string>& vString , string Delimiter)
{
	string S1;
	int count = 0;
	for (string& word : vString)
	{
		 S1 += word + Delimiter;
	}
	return (S1.substr(0,S1.length() - Delimiter.length()));
}

int main()
{
	vector<string> vString = { "Adil","chbani", "Rachid", "ali","System"};
	cout << JoinString(vString, ",");
}